/** Import from actions here **/

const defaultState = {
    initState: 'Quiver Node Reader'
};

const main = (state = defaultState, action) => {
    let stateValue = {};

    switch (action.type) {
        /** Case will be here **/

        default:
            break;
    }

    return state;
}

export default main
